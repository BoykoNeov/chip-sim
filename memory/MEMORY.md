# Memory index

Per-project memories for **chip-sim** (microchip simulator), extracted from the BigSim
program's shared memory on 2026-06-10 when the monorepo was split into standalone repos.
Cross-cutting *program* memories (bigsim-program, parallel-dev, test-execution-policy,
the notebook-discipline notes, the github-repo pointer) stayed in the BigSim archive store;
`[[links]]` to those will dangle here by design.

- [Engine unfrozen](engine-unfrozen.md) — **project (2026-06-10):** `engines.diffusion` UNFROZEN at user direction → governance **open + test-gated** (was frozen + ADR-gated); ADR 0004 = the record. The v1.x "contort the regime to fit WITHIN the frozen engine" reflex is no longer a gate (the [[chip-coupling-v12]]/[[chip-highconc-v13]]/[[litho-defocus-v14]] findings are now lineage). **Three amendments LANDED, all additive (prior invariants UNMODIFIED), no new ADR:** v1.5 native nonlinear `D(u)` (`StateDependent`+**Picard**, D(N) promoted off the lag); v1.6 explicit `forward_euler` θ=0 → [[engine-explicit-stepping-v16]]; v1.8 the 2-D regime → [[lateral-diffusion-2d]]. KEPT: `@dataclass(frozen=True)`, frozen-coefficient, sealed-surface BCs, ADRs 0001–0003. **3-D is the only remaining deferred regime.**
- [Engine explicit stepping v1.6](engine-explicit-stepping-v16.md) — **project (2026-06-11):** explicit `forward_euler` (θ=0) BUILT into `engines.diffusion`, the **second** exercise of [[engine-unfrozen]]. Gating finding (advisor-traced): explicit needs **~no new stepping code** — it falls out of the θ-method at θ=0 (CN `else` branch gives `u0+dt·(A₀u0+b0)`, implicit operator → identity), only `_METHODS` lacked the entry; the real content is the **new conditional-CFL stability invariant** the suite couldn't express before. Additive θ=0 early-return branch added (t0-only, no solve) → 28 prior invariants UNMODIFIED. Advisor: **(1) build explicit NOT 2-D** — anti-over-build, 2-D has no consumer (would guess an unvalidated API); 2-D = last deferred regime, waits for its named demo (lateral diffusion under a mask edge, lateral/vertical ≈0.8). **(2) CFL trap** — `dt_crit=1/max|diagᵢ|` off the operator, NOT textbook `Δx²/2D` (Dirichlet ghost + small cells tighten it); `Δx²/2D` anchored separately on the uniform no-flux case. `test_explicit.py` (6 tests: eigenmode decay + 1st-order-in-time / no-flux conservation / CFL boundary + BE-bounded-where-FE-explodes); engine 28→34, fast lane 195→**201**; no new ADR (ADR 0004 pre-authorizes). No chip module/notebook touched (pure engine amendment)
- [Commit at end of batch](commit-at-end-of-batch.md) — **feedback (2026-06-10):** always commit AND **push** at the end of a work batch without being asked (*"always push commits"* — supersedes the earlier "don't push unless asked"); chip-sim commits direct to `main` (solo repo, no PR flow) → `git push origin main`; clear conventional message + Co-Authored-By, fast lane green first; PRs still ask-first
- [Chip coupling v1.2](chip-coupling-v12.md) — Microchip v1.2 Phase 1↔2 back-coupling (OED + dopant segregation) BUILT 2026-06-10 (`coupling.py`, 26-test triad) — **both fit WITHIN the frozen engine** (OED = variable-`D(t)` callable; segregation = `Neumann(flux(t))` BC). **v1.2-revision: swept-sliver edge RETIRED** via a **consumer-side receding mesh** (engine untouched, no ADR; `moving_boundary=True`) → conservation upgraded identity→real magnitude (`m→∞` gain 0.137→~6e-4, P sign FLIPPED), pile-up ×1.17→×1.06. Sb=qualitative ORD edge. [[oed-source]]/[[dopant-segregation-source]]
- [Chip high-conc D(N) v1.3](chip-highconc-v13.md) — Microchip v1.3 conc-dependent diffusivity `D(N)` (the high-conc box) BUILT 2026-06-10 — **SUPERSEDED (numerics) by v1.5's native nonlinear `D(u)` path** (`StateDependent`+Picard; see [[engine-unfrozen]]); physics/demo numbers (0.34→0.76µm, ×42/×486)/scope-edge stay accurate. Cited **Fair charge-state** `D⁰+D⁻(n/nᵢ)+D⁼(n/nᵢ)²`; anomalous tail = named non-equilibrium edge (Velichko); full-activation `n_active_max` cap → physical ×42. [[dopant-charge-state-diffusivity-source]]
- [Chip defocus v1.4](litho-defocus-v14.md) — Microchip v1.4 defocus/DOF/Bossung BUILT 2026-06-10 (`litho.py` §7) — **defocus is a pure pupil PHASE → fits INSIDE litho, no new path** (`defocus_phase=exp(i(2π/λ)z(1−cosθ))`, z=0 bit-for-bit seam). **Advisor's load-bearing fix: assert the FUNDAMENTAL `4c₀c₁cosφ` NOT the contrast** — the `4c₁²cos²ψ` 2nd harmonic survives → at φ=π/2 the image **frequency-doubles**. Conservation = unitary; `k₂=0.5` paraxial. [[litho-aerial-image-source]]
- [Litho PEB v1.7](litho-peb-v17.md) — **project (2026-06-11):** PEB acid-diffusion blur BUILT (`litho.py` §8, fast lane 201→**218**) — **inverts litho's founding line**: the bake IS the engine's PDE, so the one engine-free module runs its resist back-end on `engines.diffusion` in **acid mode** (pure consumer, no ADR; **CN default** — band-limited by the optics, kept over BE ~6× a worse kernel match). Half-period symmetry cell → per-harmonic heat kernel ~2e-6; dose-conserving; σ=0 seam; **the PEB window** closes where the lens out-resolves the bake (`NA(1+σ)/4nc` λ-independent) → why BARC. [[peb-acid-diffusion-source]]
- [Litho CAR v1.9](litho-car-v19.md) — **project (2026-06-12):** CAR reaction–diffusion PEB BUILT (`litho.py` §9, fast lane 238→**254**) — **INVERTS [[litho-peb-v17]]**: the realistic chemically-amplified bake is a **coupled two-field** (acid `h`+blocked `m`) system that does NOT fit the single-field engine → built **consumer-side by operator splitting** (engine=acid sub-step, **BE not CN** — `hⁿ` non-integer NaN trap + BE keeps `∫h` exact). Cited Kirchauer §7.1.2 (APEX-E). Acid=pure catalyst (tightest leg); reaction=closed-form semigroup (no-reaction→`peb_blur` bit-for-bit). Amplification **sharpens** (a regime). Develop on deprotection `1−m`. [[peb-acid-diffusion-source]]
- [Litho Zernike v1.10](litho-zernike-v110.md) — **project (2026-06-12):** **Zernike aberrations (coma/astig/spherical) BUILT** (`litho.py` §10, 15-test mini-triad, fast lane 254→**269**) — promoted on the **SAME finding as [[litho-defocus-v14]]**: an aberration is a pure pupil **PHASE** → fits inside litho, NO new path. `Aberrations` (waves + φ_g azimuth) → `zernike_phase=exp(i·2π·W(u))`, **balanced** forms, threaded `aberrations=None` (bit-for-bit seam). **THE trap (advisor):** `fundamental_amplitude`=`4c₀c₁cosφ` for BOTH coma & defocus → can't distinguish; the discriminator is `fundamental_complex` PHASE (=the ±1 order's aberration phase exactly) → **coma = placement error** (the v1.4 "assert the right observable" analogue). Astig≡defocus PARAXIAL-only; spherical balanced=0 at rim (use interior `u=±1/√2`); **NO Strehl** (needs 2-D disk integral). Unitary leg free; loose = coma placement / astig H↔V split / spherical pitch-dependent focus. [[litho-aerial-image-source]]
- [Chip 2-D device cross-section v1.11](chip-device-2d-v111.md) — **project (2026-06-12):** the **2-D MOSFET cross-section BUILT** (`chip/device_2d.py`, 13-test triad, fast lane 273→**286**) — wires the v1.8 2-D engine ([[lateral-diffusion-2d]]) into the Phase-4 device ([[mos-threshold-voltage-source]]); the gate is the S/D's self-aligned mask → lateral encroachment shrinks the channel to `L_eff=L_drawn−2ΔL`, feeding `I_Dsat∝W/L` while **`V_t` stays long-channel** (DIBL = the named tar pit, OUT, guarded). **THE finding (advisor-framed across 3 passes): a VALIDATION deepening, not new-physics** — the independent **two-window half-cell** solve (gate centre=no-flux symmetry plane, S/D window outside) reads `L_eff_true=2x_j` directly and **CONFIRMS** the textbook subtraction + the punchthrough limit `≈2ΔL`; worth = physical grounding (real `N=N_A` crossing→hard `L_eff=0` floor) + independence (different BC topology, fails on a bug). **3 advisor corrections:** (1) my first "conservation" legs (`V_t`∥L, `I_Dsat∝1/L`) are BY-CONSTRUCTION → **regression guards not anchors**; (2) **don't oversell punchthrough** — the `max`-clamped subtraction ALSO hits 0 at `2ΔL`, they AGREE on the threshold; (3) the predicted "smooth divergence" was **grid-noise** (±1–3nm both signs) → front-interaction below the resolved scale, named not featured. `lateral=False`=exact Phase-4 seam (bit-for-bit). No engine edit, no ADR. [[lateral-diffusion-source]]
- [Lateral diffusion 2-D v1.8](lateral-diffusion-2d.md) — **project (2026-06-12):** the **2-D regime BUILT** — `engines.diffusion` gained a *new module* `diffusion2d.py` (`Diffusion2D`, tensor-product `Grid2D`/`uniform_grid_2d`, the `MaskedSurface` window/mask edge BC), the **THIRD exercise** of [[engine-unfrozen]] and the engine's **LAST deferred regime**, finally pulled in by its named consumer (`chip/diffusion_2d.py` — lateral dopant diffusion under a mask edge). Backward-Euler-only 5-point FV, `splu` cached/dt, M-matrix → 1-D guarantees carry over verbatim; **additive by construction** (imports 1-D primitives, no 1-D code path → 34 prior engine invariants UNMODIFIED), engine 34→45, fast lane 218→**238**, no new ADR. **Three findings:** (1) the problem is only genuinely 2-D because of the non-separable `MaskedSurface` BC (a windowed-IC all-no-flux drive-in is separable = outer product of two 1-D runs = hollow); (2) the tight anchor is the **dimensional-collapse seam** (2-D uniform+no-flux in one dir == 1-D engine in the other, machine precision), **NOT** the outer-product theorem (discrete BE breaks it at O(dt²) — Kronecker sum); (3) the lateral/vertical ratio is a **LOOSE** benchmark — domain-converged (2× domain bit-identical, not a wall artifact) but runs ~5–10% high vs the one cited point (model 0.87 vs cited ≈0.82 at C_B/N_s=1e-4), so device ~0.90 is the model's own value not a sourced number; validation weight = the tight erfc anchor. surface-lateral ≡ max-over-depth here. [[lateral-diffusion-source]]
- [Chip notebook flake](chip-notebook-flake.md) — slow chip.ipynb smoke test hangs **~80%** in full-gate CI (Ubuntu/Py3.12, blows the subprocess wall) → CI-only `skipif(CI)` landed (d6e4f79). **ROOT-CAUSED 2026-06-10:** an **nbclient↔ipykernel message race during EXECUTION** (unbounded per-cell iopub/idle drain) — NOT startup/teardown/chip content (runs clean ~11s the other 20%; steel-sim hung identically → shared harness). **Keep the skip** — no harness trick rescues a mid-notebook stall; removal blocked on an upstream bug. Dead ends confirmed: steel's Windows-Proactor fix doesn't transfer (Windows≠Linux cause); pinning `ipykernel<7` (6.31.0 hung too). **UPDATE 2026-06-11: load-sensitive LOCALLY too** → **THE PIN** (ADR 0003 amend): `-n auto` rides ONLY the fast lane + CI (notebook deselected/self-skips), the **local full gate stays bare serial `pytest`** — notebook never under xdist. **CLOSED DOOR (`c61c8e3`): fewer workers does NOT rescue co-scheduling** (n/2 flaked 1/4, n/4 flaked 2/6; idle cores don't protect it) — cap is an "Amdahl throughput knob only". **Do NOT re-explore fewer workers.**
- [Deal–Grove oxidation source](deal-grove-oxidation-source.md) — Microchip P2: cited Deal–Grove linear-parabolic oxide-growth rate constants (B parabolic, B/A linear; wet/dry; (111)+1.68 orientation; 0.44 Si ratio) that oxidation.py uses; thin-dry scope edge since PROMOTED → [[massoud-thin-oxide-source]]
- [Dopant charge-state diffusivity source](dopant-charge-state-diffusivity-source.md) — Microchip v1.3: cited **Fair charge-state** conc-dependent `D(N)=D⁰+D⁻(n/nᵢ)+D⁼(n/nᵢ)²` (the `D⁼` n² term drives the box) — B/P/As/Sb coefficient table (verified vs `CHARGE_STATE_TERMS`) + `nᵢ(T)=3.87e16·T^1.5·exp(−0.605/kT)` + active-carrier plateau cap `n_active_max≈3.4e20` (→ physical ×42); Plummer Ch.7 eqn 7.18/7.19 + **Fair–Tsai JECS 124:1107 (1977)** + Velichko arXiv:1905.10667; split out of [[chip-highconc-v13]] 2026-06-10 — the conc-enhanced counterpart of intrinsic [[dopant-diffusivity-source]]
- [Dopant diffusivity source](dopant-diffusivity-source.md) — Microchip P1a: Fair/Plummer intrinsic Arrhenius D(T) for B/P/As/Sb in Si (B 0.76/3.46, P 3.85/3.66 confirmed) + erfc/Gaussian forms + predep dose identity; scope edge = conc-enhanced D(N) bites predep leg → **BUILT in v1.3** [[chip-highconc-v13]] / source split to [[dopant-charge-state-diffusivity-source]]
- [Dopant mobility source](dopant-mobility-source.md) — Microchip P1a: Masetti 1983 (IEEE TED 30:764) μ(N) carrier-mobility model = the R_s conductance-integral integrand (independent of Irvin → non-circular cross-check)
- [Dopant segregation source](dopant-segregation-source.md) — Microchip v1.2: cited segregation coefficient `m = solubility_Si/solubility_SiO₂` (DEFINITION load-bearing — inverting flips deplete↔pile-up) + B 0.1–0.3 / P 10 from **Hollauer TU Wien diss. §4.1 Table 4.1 (SAME diss. as the Massoud pin)**; flux BC `J=N_surf·(0.44−1/m)·dx_ox/dt` (B deplete / P pile-up); the swept-sliver edge = pile-up ~2× inflated on the fixed grid; used by [[chip-coupling-v12]]
- [Dopant solid solubility source](dopant-solid-solubility-source.md) — Microchip P1a: Trumbore 1960 (BSTJ 39:205) solid-solubility limits = the predep Dirichlet surface N_s
- [Irvin sheet-resistance source](irvin-sheet-resistance-source.md) — Microchip P1a: Irvin 1962 (BSTJ 41:387) R_s·x_j benchmark; GRAPHICAL not callable — code computes R_s via independent μ(N)
- [Litho aerial-image source](litho-aerial-image-source.md) — Microchip P3: cited Rayleigh k₁ (0.25 two-beam / 0.5 coherent / ~0.28 best) + NILS=CD·d(lnI)/dx printability rule (Mack/lithoguru) that litho.py benchmarks against
- [PEB acid-diffusion source](peb-acid-diffusion-source.md) — Microchip v1.7: cited PEB model — `σ=√(2Dt)` Gaussian latent-image blur with **sealed-film homogeneous-Neumann BC** (Kirchauer IUE-Vienna §7.1.2 — same thesis family as the NILS floor — / Mack 1995 acid-diffusion paper), standing-wave period **λ/2n** (Mack *Lithography Tutor* Spring 1994 eq. 12 / Appl. Opt. 25:1958), the **half-period smoothing rule** σ≥λ/4n (Mack glossary), 20/40/60 nm series + 100–130 °C (lithobasics), ARC/dye/PEB mitigation list; CAR reaction–diffusion (conc-dependent `D_h`, APEX-E params) = the named scope edge; used by [[litho-peb-v17]]
- [Lateral diffusion source](lateral-diffusion-source.md) — Microchip v1.8: cited lateral-diffusion-under-a-mask-edge model — the constant-source 2-D mask-window config + the lateral/vertical junction ratio ≈ **0.75–0.85, contour-dependent** (RISES toward deeper/lower-`C_B/N_s` contours — the junction sits closer to its source at the surface), anchored on the worked point **≈0.82 at C_B/N_s=1e-4** (vert 2.8 / horiz 2.3 in 2√Dt units) after **Kennedy–O'Brien 1965** (IBM J. Res. Dev. 9:179) — but read via a **SECONDARY fab text** (ebrary), not K-O directly. The model runs **~5–10% high** vs that one point → a **LOOSE** benchmark (the tight anchor is the erfc window-column seam); Gaussian (limited-source) ratio is lower (~0.65–0.70, not firmly sourced). Replaced the memory-carried `RATIO_BAND`. Used by [[lateral-diffusion-2d]]
- [Massoud thin-oxide source](massoud-thin-oxide-source.md) — Microchip P2 v1.1 (built 2026-06-10): cited Massoud thin-dry rapid-growth constants (J.Appl.Phys 62:3416 / Hollauer TUW diss §2.7 Tables 2.3/2.4) that oxidation.py §5 uses; the COHERENT-SET rule (Ki/τi ride Massoud's own refit B/BA, never DG-1965) + the τ SIGN-TYPO finding (exp(+E/kT) not −, reproduces Fig 2.19); dry 800–1000°C only; ×1.65 on the gate recipe → ΔV_t +0.44V; plain DG stays default/bit-for-bit
- [MOS threshold-voltage source](mos-threshold-voltage-source.md) — Microchip P4: cited V_t formula + CGS constants + the MIT 6.012 PS3 P2 worked example (Vt≈0.58V, the conservation cross-check) that device.py reproduces
- [OED source](oed-source.md) — Microchip v1.2: cited OED parameterization — half-power supersaturation `Δ∝(dx_ox/dt)^0.5` (IUE-Vienna Wimmer §3.2.3 / Antoniadis-Moskowitz JAP 53:6788 1982) + fractional interstitialcy f_I (B 0.30/P 0.38/As 0.35/Sb 0.015, the dual I/V quantification paper) that `coupling.py` pins; form `D_eff/D_inert=1+f_I·Δ`, amplitude CALIBRATED-flagged; Sb f_I=0.015→≈1.05 wrong-sign residual NOT retardation; used by [[chip-coupling-v12]]
- [Fab-line game](fab-game.md) — **project (2026-06-12):** NEW direction — a gamified full-line fab simulator (sand→chip) LAYERED on chip-sim (plan `docs/plans/fab-game.md` + ADR 0005). Two layers/one repo: physics in `chip/`+`engines/` (grows CZ/Scheil as triads), game = new `fab_game/` subpackage, one-way import, split-when-mature. 7 synced choices; build slice-first. **G1 [[fab-game-g1]]; G2 [[fab-game-g2]]** (Czochralski/Scheil boule, unit-of-run RESOLVED); **G3 [[fab-game-g3]]** (wafer prep: killer-particle Poisson yield + TTV/bow geometry); **G4a [[fab-game-g4]]** (silicon purification + contamination consequence model); **G4b [[fab-game-g4b]]** (`lifetime.py` SRH τ+leakage — deep-level metals' device consequence); **G5 [[fab-game-g5]]** (etch/depo: etch bias→gate CD + step-coverage voids — the flagged-phenomenology tier); **G6 [[fab-game-g6]]** (packaging back end: cumulative assembly-yield funnel Πyᵢ + game-side speed binning — the line is now complete sand→binned chip); **G7 [[fab-game-g7]]** (roguelike framing + scoring: `scoring.py`/`game.py`, one boule = one run, Scheil V_t drift = difficulty curve; TUI deferred). **The plan's G1–G7 are all BUILT** — fab line complete sand→binned chip + a scored roguelike shell; **CG-1 [[fab-game-cg1]]** (Burton–Prim–Slichter pull-rate→k_eff, flattens the Scheil boule) and **CG-2 [[fab-game-cg2]]** (Voronkov V/G → vacancy/void COP density feeding G3 yield — CG-1's named brake, the in-model cost of fast pull) also BUILT. Remaining = the deferred TUI/tycoon + **CG-3** (Stefan moving front — the only engine-physics deepening). [[engine-unfrozen]]
- [Fab-game G4b](fab-game-g4b.md) — **project (2026-06-13):** **G4b BUILT** — `chip/lifetime.py` SRH lifetime `1/τ=1/τ_bulk+Σσ_n·v_th·N_metal` + generation leakage `J∝1/τ∝N_metal`; deep-level metals (Fe/Cu) finally become the device consequence net doping can't carry. Triad 12: tight = SRH MACHINERY (analytic = low-injection reduction of full `U(n,p)`, σ_p/E_t drop out→σ_n, a LIMIT not solver-independence; conserv = detailed-balance `U=0` at `pn=n_i²`, any params), magnitudes LOOSE/flagged (Sze/Graff σ). Leakage computed INSIDE `device_step` (provenance unchanged) + optional `SpecWindow`; metals never touch V_t. **Binding calib (advisor):** solar-1pass Cu≈2e12→1.1 nA/cm² must clear the 10 window (verified first). New flagged `"metal"` grade (Na/dopant-clean) ISOLATES metal→leaky-diode (V_t bystander); fab-game-g4b.png; rework=more passes (tiny k, k²/pass). Fast lane 401→**423**; no engine/ADR. [[fab-game-g4]]
- [Fab-game G4a](fab-game-g4.md) — **project (2026-06-13):** **G4a BUILT** — silicon purification + the Tier-1 contamination consequence model. New cited `chip/purification.py` = **Pfann single-pass** zone refining `C(u)=C₀[1−(1−k)e^(−k·u)]` (reuses czochralski's one `SEGREGATION_K`; Na added), triad 12. **Conservation REFRAMED (the trap):** single-pass omits the final-zone pile-up → `∫C` falls *short* by the closed-form swept-out deficit; "mass recovers C₀" NOT claimed (named edge) — *unlike* Scheil. `device.py` **lifts the `Q_ox=0` edge** (`ΔV_FB=−Q_ox/C_ox`, default 0 = byte-seam). `fab_game`: `PurificationKnobs` → wafer-level `Contamination`; **Na→Q_ox→V_t DOWN** (headline, lifts the edge), residual B/P→net doping, metals ride along = the **G4b gap**. Demo: MGS feed → V_t 0.374 scrapped → **rework = more passes** recovers. Default `grade="clean"` → seam. Fast lane 373→**400**; no engine amendment/ADR. **G4b now BUILT** → [[fab-game-g4b]]. [[fab-game-g3]]
- [Fab-game G1](fab-game-g1.md) — **project (2026-06-12):** **G1 BUILT** — `fab_game/` harness (state/recipe/variation/spec/steps/pipeline + demo/plots/notebook) wired through the validated diffusion→ox→litho→device back end, **ZERO new physics**; banks "one bad knob → dead die + failure trail"; 25-test mechanics suite, fast lane→**314**. **5 ADR-0005 §5 invariants green:** seam (bit-for-bit vs `demo_device`, built FIRST), determinism (one seeded rng/fixed die order), propagation-wired (device reads `t_ox`→V_t / `cd`→I_Dsat, refuses on functional fail), bookkeeping (good+bad=total/append-only/rework closes), import-guard (AST). **THE finding (corrects plan §1):** defocus's primary casualty = **NILS** not CD (CD midpoint defocus-robust; CD collapses only at EXTREME z, then I_Dsat over its *ceiling* not floor); **V_t never on the defocus chain** (device scope edge). Demo = NILS edge-ring @90nm (yield 100→68%, center-to-edge focus bowl) + literal CD/I_Dsat collapse @320nm. NO `device_2d` per die; diffusion computed once+broadcast; rework=focus-bowl-persists. [[fab-game]]
- [Fab-game G2](fab-game-g2.md) — **project (2026-06-12):** **G2 BUILT** — `chip/czochralski.py` (cited **Scheil** axial segregation `C_s(z)=N_seed·(1−z)^(k−1)` + Boule + resistivity, 12-test triad) wired into `fab_game` (`CzochralskiKnobs`, `channel_N_A` now a boule-slice **property**, `run_batch` down the boule), banked `demo_boule` artifact; fast lane 314→**338**, no engine amendment/ADR/chip-demo. **First new FRONT-OF-LINE physics** on [[fab-game]]. **THE seam fix (advisor):** parameterize by **seed-end** `N_seed≡C_s(0)` NOT melt `C_0=N_A/k` — the `x/y·y≠x` float trap (`1e17>2^53`) → `slice(0)=N_seed·1.0=N_seed` **exact** → `demo_device` seam survives. **Triad:** tight `k→1` uniform + exact seed; conservation closed-form ∫ vs quadrature [0,0.9] + full limit `∫₀¹=N_seed/k=C_0` (z→1 singularity **integrable**); loose = cited Trumbore `k` (B 0.80/P 0.35 verified, same ref as [[dopant-solid-solubility-source]]) + Masetti `ρ` reusing [[dopant-mobility-source]]. **Unit-of-run RESOLVED** (the load-bearing G2 question, plan §10): run=one wafer at `slice_z`, boule=shared context, `run_batch`=analysis VIEW not the roguelike loop; axial⊥radial. **Payoff:** boron k=0.8 → N_A↑1.585× over z=0→0.9 → V_t walks 0.547→0.747 → crosses [[mos-threshold-voltage-source]] window at z≈0.8 → tail SCRAPPED purely from substrate doping. **Oxygen→donors DEFERRED** (contested k~0.25–1.4 + calibrated kinetics → mustn't borrow Scheil anchors). [[fab-game-g1]]
- [Fab-game G3](fab-game-g3.md) — **project (2026-06-13):** **G3 BUILT** — wafer prep, the die map made physical. New cited `chip/wafer_prep.py` = **defect-limited yield** `Y=exp(−D₀A)` (Murphy/Poisson; Stapper NB `α→∞` limit; clustered-placement = named edge) + exact TTV/bow geometry (triad 14). `fab_game/defects.py` scatters killers as **per-die Poisson** against the **single** `die_area_cm2` the closed form uses → empirical kill rate **converges to the cited law** (load-bearing leg). Killer=**functional** fail; `GeometrySpec` wafer scrap + `rework_polish`. Default `defect_density=0` → seam + G1/G2 byte-unchanged. Fast lane 338→**373**; no engine amendment/ADR. [[fab-game-g2]]
- [Fab-game G5](fab-game-g5.md) — **project (2026-06-13):** **G5 BUILT** — mid-line **etch & deposition** (`chip/etch_deposition.py`, triad 14), the first **flagged-phenomenology** tier (no detailed-balance invariant). (1) etch **bias** `2·(1−A)·h·(1+OE)` shrinks resist CD→gate CD + over-etch underlayer `OE·h/S`; (2) deposition **void** when gap AR `h/(pitch−CD)` > `AR_crit=SC/(1−SC)` (PVD voids / CVD fills). **The one tight leg = the bit-for-bit seam** (`A=1`⇒bias 0; `SC=1`⇒no void); algebra = **regression guards NOT anchors** (advisor); magnitudes flagged (Wolf&Tauber/Plummer/Campbell forms). Wired: `EtchDepositionKnobs` (default=seam); `etch_deposition_step` after litho/before device **overwrites `cd_nm`** (device reads etched CD → over-etch→CD↓→`I_Dsat`↑) + `voided` **functional kill**; AR **derived from inherited gate geometry**. **Trap A handled:** etch-rate σ = **conditional 4th RNG draw** (default 0 → G1–G4 demos byte-identical). `rework_deposition` = reworkable-void/irreversible-etch contrast. CMP **& under-etch named+deferred** (no consumer / anti-over-build). Fast lane 423→**451** (+28); no engine/ADR. Next=**G6**. [[fab-game-g4]]
- [Fab-game G6](fab-game-g6.md) — **project (2026-06-13):** **G6 BUILT** — the **back end**: cumulative **assembly-yield funnel** `Y=Πyᵢ` (new cited `chip/packaging.py`, triad 15; Sze yield-ch/May&Spanos/Tummala) + game-side **speed binning**. Line now complete **sand→binned chip**; last physics G-step. **Triad:** tight=`yᵢ=1` bit-exact seam + multiplicativity identity — BUT the algebra is **structural**; the non-circular leg is the **game-side Bernoulli→Πyᵢ LLN convergence** (like [[fab-game-g3]] placement→`exp(−D₀A)`); benchmark=flagged `ASSEMBLY_STEPS`. **THE advisor trap:** binning is **NOT physics** (a grading policy) → dropped the Gaussian `bin_fractions` (near-tautological + realized `I_Dsat` non-Gaussian = a dishonest leg) → `chip/packaging.py` is **single-headed** (funnel only); `SpeedBins` (default one open bin = seam) lives in `spec.py`. Wired: `PackagingKnobs` (4 yields, default 1.0); `packaging_step` **after** `test`; assembly kill = per-die **Bernoulli gated on `Y<1 ∧ variation.enabled`, drawn LAST** → **G1–G5 byte-identical** ([[fab-game-g5]] trap again); binning sorts survivors by `I_Dsat` (speed proxy). `Die`+`assembled`/`bin`; **four-way partition** {front-end fail/assembly scrap/bin-out/binned-good}, front-end fails NOT packaged. Pedagogy: spread→value distribution (tight→premium; loose→spread+bin-out tail) + "works but never shipped" (assembly scrap, perfect front end). **Advisor done-check caught a latent rework bug:** back-end deaths (`assembled` set) must be excluded from `rework_litho`/`rework_polish` (else a cracked die resurrects) — fixed. `demo_packaging`/`fab-game-g6.png`. Fast lane 451→**476** (+25); no engine/ADR/gallery card; **rebond named+deferred** (cracked=scrap). Next=**G7** roguelike shell. [[fab-game-g5]]
- [Fab-game G7](fab-game-g7.md) — **project (2026-06-13):** **G7 BUILT — framing + scoring; TUI DEFERRED.** The roguelike **game shell**, the **last** G-step. **Purely additive game policy** — no physics/`run_line`/`WaferState` touched → 476 green trivially, **no seam, no ADR**. `fab_game/scoring.py` (bin **prices** + costs → `ScoreCard`, `revenue=Σ price·count` over shipped dies, monotone) + `fab_game/game.py` (immutable `GameSession`/`GameConfig`, `process_wafer`/`scrap_wafer`/`play`, `MARKET_BINS` real multi-grade, `ReworkSpec`). **Framing (physics-grounded, advisor-affirmed): one boule = one run, each wafer a turn, the [[fab-game-g2]] Scheil V_t drift IS the difficulty curve** (N_A↑→V_t 0.55→0.75 out the ceiling by z≈0.8). Reuse `run_line` per wafer (do NOT refactor to per-step). **Lever = thin the gate oxide** (lower t_ox → lower V_t + higher I_Dsat → premium). **Build finding (corrected mid-build): the lever has an in-model Goldilocks LIMIT** — over-thinning the extreme tail drives I_Dsat into its ceiling (the linear trim fumbles z=0.9: mean I_Dsat≈4.19 vs 4.2) → NOT one-sided; **plus** a separate *unmodeled* cost (no gate-oxide reliability/leakage penalty — named scope edge). **TUI deferred (advisor: defer, don't ask)** — user said "framing/**scoring**"; "roguelike framing" = a session model, not a UI (all headless/testable); a Textual front-end is a thin driver, added when wanted. `demo_game`/`fab-game-g7.png`: 3 strategies down one boule, **naive<scrap<adapt**. Mechanics-tested: determinism, bookkeeping closes, monotonicity, sandbox-vs-roguelike (one mode flag), drift arc. Fast lane 476→**492** (+16). **G1–G7 all BUILT** — fab line complete + scored shell; remaining = deferred TUI/tycoon + CG-1/2/3. [[fab-game-g6]]
- [Fab-game CG-1](fab-game-cg1.md) — **project (2026-06-13):** **CG-1 BUILT** — first Czochralski crystal-growth deepening (G2 follow-on, plan §6a). New cited physics `chip/czochralski.py` §1b (additive): **Burton–Prim–Slichter** `effective_segregation_coefficient(k₀,Δ)=k₀/[k₀+(1−k₀)e^(−Δ)]` + `normalized_growth_velocity(pull_rate)→Δ=v·δ/D` (flagged δ/D). Pull rate lifts k_eff→1 → **flattens the Scheil drift**. **Triad: the two LIMITS tight** (Δ=0⇒k₀ bit-exact seam; Δ→∞⇒1 trapping; k₀=1⇒1), **no conservation law** (etch/packaging tier); machinery = monotone-bounded-[k₀,1] + identity `1/k_eff−1=(1/k₀−1)e^(−Δ)`; benchmark = cited BPS form, k₀ tight Trumbore, δ/D flagged. **Wired ONE knob, opt-in:** `CzochralskiKnobs.pull_rate_mm_min=None` → `Recipe.boule` k=k_eff if set else k₀ → **G2/G7 demos byte-identical** (binding seam = `demo_boule` z>0 sweep). Fast lane 492→**504** (+12); no engine/ADR. **THE honesty (advisor, magnitude-check-first):** boron k₀=0.80 barely segregates → realistic Si pull (≤2 mm/min) only modest (k_eff≈0.84, V_t walk +0.20→+0.155); near-flat needs ~10–20 mm/min **labelled illustrative**. **NOT a score-war** — CG-1 one-sided in-model (no brake), so demo = physics consequence only; the cost (CG-2 microvoids/striations/dislocation) named **loudly deferred**. `demo_crystal_growth`/`fab-game-cg1.png`. CG-2/3 deferred. [[fab-game-g2]]
- [Fab-game CG-2](fab-game-cg2.md) — **project (2026-06-13):** **CG-2 BUILT** — Voronkov `V/G` grown-in defect criterion (`chip/czochralski.py` §1c, cited; `ξ>ξ_t`⇒vacancy/void COP killers), wired opt-in → vacancy-side void density feeds the **same G3 Poisson map**; **the in-model brake CG-1 lacked** (pull faster → voids → yield↓). Default seam-safe (`G=None`⇒`+0.0`). Honest finding (advisor): CG-1 benefit FLAT in range → CG-2 criterion ALONE sets the pull (no trade-off; boundary edge = throughput, unmodeled). Fast lane 506→**522**. [[fab-game-cg1]] [[fab-game-g3]]
- [Gallery local edition](gallery-local-edition.md) — **project (2026-06-12, `5d5f97e`):** the gallery now renders **two** committed pages — `docs/index.html` (public, GitHub-linked, Pages-served, kept byte-identical) + **`docs/index.local.html`** (links open in a *running* JupyterLab `localhost:8888/lab/tree/...` → the notebook card is a real **click→live-launch**, the thing GitHub's read-only viewer can't do). User is a **standalone-Jupyter** (not VS Code) user; chose "keep public + add local page" over torching index.html. **Hard fact:** a browser click can't *spawn* `jupyter lab` (only reach a running one) → caveats in-card. Fixed localhost strings = no machine path → both golden-tested. **Don't delete index.local.html / don't mix github↔localhost across the two** (tests guard both)
