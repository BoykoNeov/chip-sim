# Memory index

Per-project memories for **chip-sim** (microchip simulator), extracted from the BigSim
program's shared memory on 2026-06-10 when the monorepo was split into standalone repos.
Cross-cutting *program* memories (bigsim-program, parallel-dev, test-execution-policy,
the notebook-discipline notes, the github-repo pointer) stayed in the BigSim archive store;
`[[links]]` to those will dangle here by design.

- [Engine unfrozen](engine-unfrozen.md) — **project (2026-06-10):** `engines.diffusion` UNFROZEN at user direction ("unfreeze all engines in the contract") → governance **open + test-gated** (was frozen + ADR-gated). **The v1.x reflex "contort the regime to fit WITHIN the frozen engine" is no longer a constraint** — native `D(u)`/2-D/explicit MAY now be built directly into the engine (still suite-gated, must not silently break a consumer). **FIRST AMENDMENT LANDED (v1.5, 2026-06-10): native nonlinear `D(u)` is BUILT** — `StateDependent(func)` + **Picard** (NOT Newton — inherits the engine's per-iterate max-principle + conservation); v1.3's `D(N)` promoted off the consumer-side lag onto it (`_diffuse_dn` now a thin `StateDependent` step-loop, `picard_iters` knob gone); **additive** (only `StateDependent` enters the loop → 18 prior tests UNMODIFIED), invariant 6 + `Not in v1` line flipped in CONTRACT.md, `test_nonlinear_d.py` (engine 18→28, fast lane 188), no new ADR. The "fits within the frozen engine" findings of [[chip-coupling-v12]]/[[chip-highconc-v13]]/[[litho-defocus-v14]] are now historical lineage, not a live gate. ADR 0004 = the record; commits `4536ec0`/`26bf0c9`/`ae07db4` (CONTRACT.md + ADR + chip narrative); KEPT: `@dataclass(frozen=True)`, numerical frozen-coefficient, sealed-surface BCs, ADRs 0001–0003. steel/planet copies untouched. **SECOND AMENDMENT LANDED (v1.6, 2026-06-11): explicit `forward_euler` (θ=0) is BUILT** → [[engine-explicit-stepping-v16]]. **THIRD AMENDMENT LANDED (v1.8, 2026-06-12): the 2-D regime is BUILT** → [[lateral-diffusion-2d]] (new module `diffusion2d.py`, additive, 34 prior invariants UNMODIFIED); `Not built` now just **3-D** (nonlinear `D(u)` ✓v1.5, explicit ✓v1.6, 2-D ✓v1.8 — **3-D is the only remaining deferred regime**).
- [Engine explicit stepping v1.6](engine-explicit-stepping-v16.md) — **project (2026-06-11):** explicit `forward_euler` (θ=0) BUILT into `engines.diffusion`, the **second** exercise of [[engine-unfrozen]]. Gating finding (advisor-traced): explicit needs **~no new stepping code** — it falls out of the θ-method at θ=0 (CN `else` branch gives `u0+dt·(A₀u0+b0)`, implicit operator → identity), only `_METHODS` lacked the entry; the real content is the **new conditional-CFL stability invariant** the suite couldn't express before. Additive θ=0 early-return branch added (t0-only, no solve) → 28 prior invariants UNMODIFIED. Advisor: **(1) build explicit NOT 2-D** — anti-over-build, 2-D has no consumer (would guess an unvalidated API); 2-D = last deferred regime, waits for its named demo (lateral diffusion under a mask edge, lateral/vertical ≈0.8). **(2) CFL trap** — `dt_crit=1/max|diagᵢ|` off the operator, NOT textbook `Δx²/2D` (Dirichlet ghost + small cells tighten it); `Δx²/2D` anchored separately on the uniform no-flux case. `test_explicit.py` (6 tests: eigenmode decay + 1st-order-in-time / no-flux conservation / CFL boundary + BE-bounded-where-FE-explodes); engine 28→34, fast lane 195→**201**; no new ADR (ADR 0004 pre-authorizes). No chip module/notebook touched (pure engine amendment)
- [Commit at end of batch](commit-at-end-of-batch.md) — **feedback (2026-06-10):** always commit AND **push** at the end of a work batch without being asked (*"always push commits"* — supersedes the earlier "don't push unless asked"); chip-sim commits direct to `main` (solo repo, no PR flow) → `git push origin main`; clear conventional message + Co-Authored-By, fast lane green first; PRs still ask-first
- [Chip coupling v1.2](chip-coupling-v12.md) — Microchip v1.2 Phase 1↔2 back-coupling (OED + dopant segregation) BUILT 2026-06-10 (`coupling.py`, 19-test triad, gate 152): the decisive finding = **both fit WITHIN the frozen engine** (OED = the already-blessed variable-`D(t)` callable, NOT the unbuilt `D(N)`; segregation = a `Neumann(flux(t))` BC); unified degenerate seam `dx_ox/dt→0`→`drive_in` (advisor dropped the wrong `m→∞` anchor); **the swept-sliver scope edge** = segregation flux is a moving-INTERFACE balance on a NON-moving grid → `0.44·R` recession double-counted → boron depletion robust / phosphorus pile-up ~2× high (the `m→∞` inert-oxide diagnostic pins ~10% spurious dose gain), coupled "conservation"=accounting identity not magnitude; Sb=qualitative ORD edge; [[oed-source]]/[[dopant-segregation-source]]. **UPDATE 2026-06-11 (v1.2-revision): swept-sliver edge RETIRED** — the "Stefan problem the engine can't express" was a **consumer-side receding mesh** (engine UNTOUCHED, no ADR): recede the silicon domain to `s=0.44·(x_ox−x_initial)` each sub-step via a truncated-first-cell `grid_from_edges` sub-grid, keep the SAME flux (the mesh motion supplies the missing `−v·N_surf` Leibniz term; `dQ_Si/dt=−C_ox·R`). Default `moving_boundary=True` (False=legacy "before"). **Conservation upgraded identity→real magnitude** (`m→∞` now conserves, gain 0.137→~6e-4 O(dt); `oxide_uptake`=real `∫C_ox·R dt` within a few %, **≥0 for both** — phosphorus uptake sign FLIPPED); demo pile-up **×1.17→×1.06**, boron ×0.33 unchanged, surface receded 9.1nm; `CoupledResult`+`interface_depth`/`surface_index`; suite 19→26, fast lane 188→**195**. Advisor timed out twice → Leibniz derivation verified empirically.
- [Chip high-conc D(N) v1.3](chip-highconc-v13.md) — Microchip v1.3 concentration-dependent diffusivity D(N), the high-concentration box, BUILT 2026-06-10 (`diffusion_highconc.py`, 14-test triad, fast lane 163): the v1.3 finding = D(N) fits within the (then-frozen) engine via a consumer-side lagged-coefficient closure — **SUPERSEDED (numerics) by the v1.5 promotion onto the engine's native nonlinear `D(u)` path** (`StateDependent`+Picard; holder/`picard_iters` gone; CONTRACT amended), see [[engine-unfrozen]]; the **physics/model/demo numbers (0.34→0.76µm, ×42/×486)/scope-edge stay accurate** — cited **Fair charge-state** `D⁰+D⁻(n/n_i)+D⁼(n/n_i)²` (Plummer Ch.7 / Fair–Tsai 1977; P `D⁰` = the Phase-1a intrinsic value, B the exception); box front + deeper x_j captured, **anomalous tail/kink = the named non-equilibrium scope edge** (Velichko 2019); conservation reframed as a machinery check; full-activation n=N flagged (the n_active_max plateau cap → physical ×42 vs uncapped ×486); the flatten's 6 sibling demos mis-saving figures one dir up = **now FIXED** (parents[2]→[1], commit db79f6b); [[dopant-diffusivity-source]]
- [Chip defocus v1.4](litho-defocus-v14.md) — Microchip v1.4 lithographic defocus / depth of focus / Bossung BUILT 2026-06-10 (`litho.py` §7, 16-test mini-triad, chip fast lane 161 / +16): the decisive finding = **defocus is a pure pupil PHASE → fits INSIDE the litho module, no new path** (`coherent_image` already sums complex amps; `defocus_phase = exp(i·(2π/λ)·z·(1−cosθ))` on the full pupil coord `f_m+f_s`, threaded `defocus_nm=0.0`; z=0→1.0 bit-for-bit seam); **the advisor's load-bearing fix = assert the FUNDAMENTAL `4c₀c₁cosφ` (machine precision) NOT the contrast** — the image is `c₀²+4c₁²cos²ψ+4c₀c₁cosφ·cosψ`, the `4c₁²cos²ψ` 2nd harmonic survives → at the φ=π/2 null the contrast does NOT vanish, the image **frequency-doubles** (pinned positive); conservation = **defocus is unitary** (`mean=Σ|c_m|²=transmitted_power` ∀z); `k₂=0.5` DERIVED from the φ=π/2 null but **paraxial** (exact full-cosθ null ±119 vs DOF ±134 at NA 0.85, converges as NA→0); symmetric-dipole infinite-DOF bit-for-bit; next litho promotion = PEB acid-diffusion blur (= a diffusion solve → frozen-engine reuse angle); [[litho-aerial-image-source]]
- [Litho PEB v1.7](litho-peb-v17.md) — **project (2026-06-11):** PEB acid-diffusion blur BUILT (`litho.py` §8, 17-test mini-triad, fast lane 201→**218**) — the finding **inverts litho's founding line**: the bake IS the engine's PDE, so the one engine-free module now runs its resist back-end on `engines.diffusion` in **acid mode** (pure consumer, NO amendment/ADR; **CN default — load-bearing reason is band-limiting by the optics, NOT temporal accuracy; kept over BE, which is ~6× a worse kernel match, 2026-06-11 review**); **half-period symmetry cell** (no-flux faces = the even image's mirror planes; Neumann eigenmodes = its cosine harmonics) → the bounded solve IS the periodic blur, per-harmonic heat kernel `exp(−2π²k²σ²/p²)` engine-vs-closed-form ~2e-6; bake conserves dose → the v1 power balance AND the default mean-clip dose are **blur-invariant** (~6e-14); σ=0 bit-for-bit seam + σ→0 continuity; asymmetric images (off-axis pole + defocus) REFUSED not mis-blurred; **the PEB window** [λ/4n≈28, 45] nm at 240 nm pitch, closing at `p_close=λ/4nc`≈151 nm; **coincidence SHARPENED 2026-06-11**: `p_close` is NA-INDEPENDENT, the partial-coherence cutoff `λ/(NA(1+σ))`≈151 slides with the lens → ratio `NA(1+σ)/4nc` is **λ-independent** (1.0006 here, a coincidence of two INDEPENDENT groups WITH an NA-mechanism — not "mechanism instead of coincidence", not the advisor's `NA/2nc`=1.33 two-beam factor) — at NA 0.93 cutoff slides to ~138 while `p_close` pinned → **lens out-resolves the bake** (145 nm images fine, rule-abiding bake keeps 0.47) → why BARC; user said "Litho v1.5" → repo-global counter made it **v1.7** (v1.5/v1.6 = the engine amendments); advisor disabled this session; [[peb-acid-diffusion-source]]
- [Litho CAR v1.9](litho-car-v19.md) — **project (2026-06-12):** **CAR reaction–diffusion PEB BUILT** (`litho.py` §9, 16-test mini-triad, fast lane 238→**254**) — v1.7's §8-named CAR scope edge, promoted. **INVERTS [[litho-peb-v17]]'s finding**: the linear blur WAS the engine's pure single-field PDE, but the realistic chemically-amplified bake is a **coupled two-field** (acid `h` + blocked `m`) reaction–diffusion system that does **NOT** fit the single-field engine → built **consumer-side by operator splitting** (engine = acid-diffusion sub-step only, array-`D`; reaction in closed form), back to the v1.2 pattern. **No engine amendment/ADR.** Cited Kirchauer §7.1.2 2-field model (`∂m/∂t=−k_amp·m·hⁿ`, `∂h/∂t=−k_loss·h+∂ₓ(D_h(m)∂ₓh)`, APEX-E @ 90 °C). **4 advisor findings:** (1) splitting FORCED (loss ∝`u`, 2nd field, `D_h(m)` not `D_h(h)`); (2) diffusion sub-step is **BE not v1.7's CN** — `hⁿ` non-integer-`n` **NaN trap** + BE max-principle keeps `∫h` conservation **exact** (CN ring→mass-adding clamp); (3) acid is a **pure catalyst** (no `h·m` sink → `∫h` conserved/exact-`e^{−k_loss·t}`-decay, the tightest leg); (4) reaction **closed-form semigroup** → flat-field anchor **machine-precision-exact** + no-reaction short-circuits to `peb_blur` **bit-for-bit**. Loose: **amplification sharpens** (superlinear `hⁿ` edge steeper than acid, NILS 3.4→4.5; a REGIME not a law) vs diffusion/loss/over-bake degrade. Cited tiny `D` (σ~nm) ⇒ diffusion **negligible vs ~150 nm optics** (CAR makes sharp features). Develop on **deprotection `1−m`** (chemically-faithful, vs v1.7's acid clip). [[peb-acid-diffusion-source]]
- [Litho Zernike v1.10](litho-zernike-v110.md) — **project (2026-06-12):** **Zernike aberrations (coma/astigmatism/spherical) BUILT** (`litho.py` §10, 15-test mini-triad, fast lane 254→**269**) — Phase 3's "aberration-free pupil apart from defocus" edge, promoted on the **SAME finding as [[litho-defocus-v14]]**: a Zernike aberration is a pure pupil **PHASE** → fits inside the litho module, NO new path (`coherent_image` sums complex amps). `Aberrations` dataclass (coma/astig/spherical in **waves** + `grating_azimuth_deg` φ_g) + `zernike_phase = exp(i·2π·W(u))` on the 1-D pupil slice `u=f_total/f_cut` (**balanced** Zernike: coma `(3u³−2u)cosφ_g` ODD, astig `u²cos2φ_g` EVEN, spherical `6u⁴−6u²` EVEN), threaded `aberrations=None` (bit-for-bit seam), **separate from `defocus_nm`** (waves/paraxial vs v1.4 exact `1−cosθ`). **THE load-bearing trap (advisor):** `fundamental_amplitude` returns `4c₀c₁cosφ` for BOTH coma and defocus → CAN'T distinguish them; the discriminator is `fundamental_complex` whose **PHASE = the ±1 order's aberration phase exactly** (0 defocus / coma-shift coma, machine precision) → **coma = placement error** (the v1.4 "assert the right observable" analogue). **Other traps:** spherical balanced `6u⁴−6u²`=**0 at rim** → even-invariance dipole test trivial there, use **interior pair u=±1/√2**; astig≡defocus is **PARAXIAL only** (`u²` vs exact `1−cosθ`); balanced forms + the **one φ_g scalar** make astig's **H↔V best-focus split** testable; **NO Strehl asserted** (needs 2-D pupil-disk integral, λ/14 only as scale — discrete-1-D caveat). Triad: bit-for-bit seam + **parity pair** (even→dipole invariant, odd coma→pure shift) + coma↔defocus phase discriminator (tight); **unitary** `mean=Σ|c_m|²=transmitted_power` ∀coeff (the v1.4 leg free); loose = coma placement+PEB-refusal / astig H↔V split / spherical pitch-dependent focus. [[litho-aerial-image-source]]
- [Chip 2-D device cross-section v1.11](chip-device-2d-v111.md) — **project (2026-06-12):** the **2-D MOSFET cross-section BUILT** (`chip/device_2d.py`, 13-test triad, fast lane 273→**286**) — wires the v1.8 2-D engine ([[lateral-diffusion-2d]]) into the Phase-4 device ([[mos-threshold-voltage-source]]); the gate is the S/D's self-aligned mask → lateral encroachment shrinks the channel to `L_eff=L_drawn−2ΔL`, feeding `I_Dsat∝W/L` while **`V_t` stays long-channel** (DIBL = the named tar pit, OUT, guarded). **THE finding (advisor-framed across 3 passes): a VALIDATION deepening, not new-physics** — the independent **two-window half-cell** solve (gate centre=no-flux symmetry plane, S/D window outside) reads `L_eff_true=2x_j` directly and **CONFIRMS** the textbook subtraction + the punchthrough limit `≈2ΔL`; worth = physical grounding (real `N=N_A` crossing→hard `L_eff=0` floor) + independence (different BC topology, fails on a bug). **3 advisor corrections:** (1) my first "conservation" legs (`V_t`∥L, `I_Dsat∝1/L`) are BY-CONSTRUCTION → **regression guards not anchors**; (2) **don't oversell punchthrough** — the `max`-clamped subtraction ALSO hits 0 at `2ΔL`, they AGREE on the threshold; (3) the predicted "smooth divergence" was **grid-noise** (±1–3nm both signs) → front-interaction below the resolved scale, named not featured. `lateral=False`=exact Phase-4 seam (bit-for-bit). No engine edit, no ADR. [[lateral-diffusion-source]]
- [Lateral diffusion 2-D v1.8](lateral-diffusion-2d.md) — **project (2026-06-12):** the **2-D regime BUILT** — `engines.diffusion` gained a *new module* `diffusion2d.py` (`Diffusion2D`, tensor-product `Grid2D`/`uniform_grid_2d`, the `MaskedSurface` window/mask edge BC), the **THIRD exercise** of [[engine-unfrozen]] and the engine's **LAST deferred regime**, finally pulled in by its named consumer (`chip/diffusion_2d.py` — lateral dopant diffusion under a mask edge). Backward-Euler-only 5-point FV, `splu` cached/dt, M-matrix → 1-D guarantees carry over verbatim; **additive by construction** (imports 1-D primitives, no 1-D code path → 34 prior engine invariants UNMODIFIED), engine 34→45, fast lane 218→**238**, no new ADR. **Three findings:** (1) the problem is only genuinely 2-D because of the non-separable `MaskedSurface` BC (a windowed-IC all-no-flux drive-in is separable = outer product of two 1-D runs = hollow); (2) the tight anchor is the **dimensional-collapse seam** (2-D uniform+no-flux in one dir == 1-D engine in the other, machine precision), **NOT** the outer-product theorem (discrete BE breaks it at O(dt²) — Kronecker sum); (3) the lateral/vertical ratio is a **LOOSE** benchmark — domain-converged (2× domain bit-identical, not a wall artifact) but runs ~5–10% high vs the one cited point (model 0.87 vs cited ≈0.82 at C_B/N_s=1e-4), so device ~0.90 is the model's own value not a sourced number; validation weight = the tight erfc anchor. surface-lateral ≡ max-over-depth here. [[lateral-diffusion-source]]
- [Chip notebook flake](chip-notebook-flake.md) — slow chip.ipynb smoke test hangs **~80%** in full-gate CI (Ubuntu/Py3.12, blows the subprocess wall) → CI-only `skipif(CI)` landed (d6e4f79). **ROOT-CAUSED 2026-06-10 (4 CI diag rounds):** an **nbclient↔ipykernel-7.x message race during EXECUTION** (unbounded per-cell iopub/idle drain on 7.x threaded channels) — NOT startup, NOT teardown, NOT chip content (notebook runs clean in ~11s the other 20%; steel-sim hung identically → shared harness). **The teardown-skip fix (explicit cell loop + os._exit) FAILED verification** (still 8/10) — no harness trick rescues a mid-notebook stall. **Recommendation: keep the skip** (reach-layer test, physics validated behind triads; removal blocked on an upstream bug). **RE-CONFIRMED 2026-06-10 vs steel-sim's notebook-kernel-wedge handoff:** steel's Windows-Proactor/retry fix does NOT transfer (steel §6: Windows cause ≠ Linux CI hang, keep the skip); pinning `ipykernel<7` is a **re-confirmed dead end** — CI run 27299430919 ran ipykernel 6.31.0, hung 240s wall on attempt 1/10 (so "7.x-specific" is misleading — 6.31.0 hangs too). De-staled the in-repo comments (dropped the wrong "startup-hang → force SelectorEventLoop" candidate) + added a `CHIP_NOTEBOOK_FORCE_CI=1` re-test lever on main. main still carries the skip; experiment branch deleted. **UPDATE 2026-06-11: load-sensitive LOCALLY too** — while adding pytest-xdist (commit `fa56f81`) the notebook timed out locally *once* when its `-m slow` leg launched right after a parallel `-n auto` bulk (4/4 in isolation) → the "reliable locally" framing was an under-load blind spot, and this is the load-bearing reason for **THE PIN** (ADR 0003 amend): `-n auto` rides only the fast lane + CI (notebook deselected/self-skips), the **local full gate stays bare serial `pytest`** — notebook never under xdist. **CLOSED DOOR (2026-06-11, commit `c61c8e3`): reducing the worker count does NOT rescue co-scheduling** — n/2 (8 workers) flaked 1/4, n/4 (4 workers, 12 idle cores) flaked 2/6; the cap value never governs the notebook (the hook fires only on the fast lane, where it's deselected). The "idle half = headroom for the notebook" rationale was WRONG and was corrected to "Amdahl throughput knob only" across conftest/pyproject/README/ADR. **Do NOT re-explore fewer workers.**
- [Deal–Grove oxidation source](deal-grove-oxidation-source.md) — Microchip P2: cited Deal–Grove linear-parabolic oxide-growth rate constants (B parabolic, B/A linear; wet/dry; (111)+1.68 orientation; 0.44 Si ratio) that oxidation.py uses; thin-dry scope edge since PROMOTED → [[massoud-thin-oxide-source]]
- [Dopant charge-state diffusivity source](dopant-charge-state-diffusivity-source.md) — Microchip v1.3: cited **Fair charge-state** conc-dependent `D(N)=D⁰+D⁻(n/nᵢ)+D⁼(n/nᵢ)²` (the `D⁼` n² term drives the box) — B/P/As/Sb coefficient table (verified vs `CHARGE_STATE_TERMS`) + `nᵢ(T)=3.87e16·T^1.5·exp(−0.605/kT)` + active-carrier plateau cap `n_active_max≈3.4e20` (→ physical ×42); Plummer Ch.7 eqn 7.18/7.19 + **Fair–Tsai JECS 124:1107 (1977)** + Velichko arXiv:1905.10667; split out of [[chip-highconc-v13]] 2026-06-10 — the conc-enhanced counterpart of intrinsic [[dopant-diffusivity-source]]
- [Dopant diffusivity source](dopant-diffusivity-source.md) — Microchip P1a: Fair/Plummer intrinsic Arrhenius D(T) for B/P/As/Sb in Si (B 0.76/3.46, P 3.85/3.66 confirmed) + erfc/Gaussian forms + predep dose identity; scope edge = conc-enhanced D(N) bites predep leg → **BUILT in v1.3** [[chip-highconc-v13]] / source split to [[dopant-charge-state-diffusivity-source]]
- [Dopant mobility source](dopant-mobility-source.md) — Microchip P1a: Masetti 1983 (IEEE TED 30:764) μ(N) carrier-mobility model = the R_s conductance-integral integrand (independent of Irvin → non-circular cross-check)
- [Dopant segregation source](dopant-segregation-source.md) — Microchip v1.2: cited segregation coefficient `m = solubility_Si/solubility_SiO₂` (DEFINITION load-bearing — inverting flips deplete↔pile-up) + B 0.1–0.3 / P 10 from **Hollauer TU Wien diss. §4.1 Table 4.1 (SAME diss. as the Massoud pin)**; flux BC `J=N_surf·(0.44−1/m)·dx_ox/dt` (B deplete / P pile-up); the swept-sliver edge = pile-up ~2× inflated on the fixed grid; used by [[chip-coupling-v12]]
- [Dopant solid solubility source](dopant-solid-solubility-source.md) — Microchip P1a: Trumbore 1960 (BSTJ 39:205) solid-solubility limits = the predep Dirichlet surface N_s
- [Irvin sheet-resistance source](irvin-sheet-resistance-source.md) — Microchip P1a: Irvin 1962 (BSTJ 41:387) R_s·x_j benchmark; GRAPHICAL not callable — code computes R_s via independent μ(N)
- [Litho aerial-image source](litho-aerial-image-source.md) — Microchip P3: cited Rayleigh k₁ (0.25 two-beam / 0.5 coherent / ~0.28 best) + NILS=CD·d(lnI)/dx printability rule (Mack/lithoguru) that litho.py benchmarks against
- [PEB acid-diffusion source](peb-acid-diffusion-source.md) — Microchip v1.7: cited PEB model — `σ=√(2Dt)` Gaussian latent-image blur with **sealed-film homogeneous-Neumann BC** (Kirchauer IUE-Vienna §7.1.2 — same thesis family as the NILS floor — / Mack 1995 acid-diffusion paper), standing-wave period **λ/2n** (Mack *Lithography Tutor* Spring 1994 eq. 12 / Appl. Opt. 25:1958), the **half-period smoothing rule** σ≥λ/4n (Mack glossary), 20/40/60 nm series + 100–130 °C (lithobasics), ARC/dye/PEB mitigation list; CAR reaction–diffusion (conc-dependent `D_h`, APEX-E params) = the named scope edge; used by [[litho-peb-v17]]
- [Lateral diffusion source](lateral-diffusion-source.md) — Microchip v1.8: cited lateral-diffusion-under-a-mask-edge model — the constant-source 2-D mask-window config + the lateral/vertical junction ratio ≈ **0.75–0.85, contour-dependent** (RISES toward deeper/lower-`C_B/N_s` contours — the junction sits closer to its source at the surface), anchored on the worked point **≈0.82 at C_B/N_s=1e-4** (vert 2.8 / horiz 2.3 in 2√Dt units) after **Kennedy–O'Brien 1965** (IBM J. Res. Dev. 9:179) — but read via a **SECONDARY fab text** (ebrary), not K-O directly. The model runs **~5–10% high** vs that one point → a **LOOSE** benchmark (the tight anchor is the erfc window-column seam); Gaussian (limited-source) ratio is lower (~0.65–0.70, not firmly sourced). Replaced the memory-carried `RATIO_BAND`. Used by [[lateral-diffusion-2d]]
- [Massoud thin-oxide source](massoud-thin-oxide-source.md) — Microchip P2 v1.1 (built 2026-06-10): cited Massoud thin-dry rapid-growth constants (J.Appl.Phys 62:3416 / Hollauer TUW diss §2.7 Tables 2.3/2.4) that oxidation.py §5 uses; the COHERENT-SET rule (Ki/τi ride Massoud's own refit B/BA, never DG-1965) + the τ SIGN-TYPO finding (exp(+E/kT) not −, reproduces Fig 2.19); dry 800–1000°C only; ×1.65 on the gate recipe → ΔV_t +0.44V; plain DG stays default/bit-for-bit
- [MOS threshold-voltage source](mos-threshold-voltage-source.md) — Microchip P4: cited V_t formula + CGS constants + the MIT 6.012 PS3 P2 worked example (Vt≈0.58V, the conservation cross-check) that device.py reproduces
- [OED source](oed-source.md) — Microchip v1.2: cited OED parameterization — half-power supersaturation `Δ∝(dx_ox/dt)^0.5` (IUE-Vienna Wimmer §3.2.3 / Antoniadis-Moskowitz JAP 53:6788 1982) + fractional interstitialcy f_I (B 0.30/P 0.38/As 0.35/Sb 0.015, the dual I/V quantification paper) that `coupling.py` pins; form `D_eff/D_inert=1+f_I·Δ`, amplitude CALIBRATED-flagged; Sb f_I=0.015→≈1.05 wrong-sign residual NOT retardation; used by [[chip-coupling-v12]]
- [Fab-line game](fab-game.md) — **project (2026-06-12):** NEW direction — a gamified full-line fab simulator (sand→packaged chip) LAYERED on chip-sim (plan `docs/plans/fab-game.md` + ADR 0005 drafted). Two layers, one repo: physics stays in `chip/`+`engines/` (grows CZ/Scheil etc. as cited triads), game = new `fab_game/` subpackage (WaferState, yield, rework, UI), one-way import, split-when-mature. 7 synced choices (grand tour / both-failure / boule→batch + die-map / continuous knobs / realistic rework / roguelike+sandbox / NO game engine). Build **slice-first**: G1 = harness on the validated diffusion→ox→litho→device back end, then G2 Scheil. OPEN: single-wafer-vs-boule unit-of-run. [[engine-unfrozen]]
- [Gallery local edition](gallery-local-edition.md) — **project (2026-06-12, `5d5f97e`):** the gallery now renders **two** committed pages — `docs/index.html` (public, GitHub-linked, Pages-served, kept byte-identical) + **`docs/index.local.html`** (links open in a *running* JupyterLab `localhost:8888/lab/tree/...` → the notebook card is a real **click→live-launch**, the thing GitHub's read-only viewer can't do). User is a **standalone-Jupyter** (not VS Code) user; chose "keep public + add local page" over torching index.html. **Hard fact:** a browser click can't *spawn* `jupyter lab` (only reach a running one) → caveats in-card. Fixed localhost strings = no machine path → both golden-tested. **Don't delete index.local.html / don't mix github↔localhost across the two** (tests guard both)
