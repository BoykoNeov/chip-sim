# Memory index

Per-project memories for **chip-sim** (microchip simulator), extracted from the BigSim
program's shared memory on 2026-06-10 when the monorepo was split into standalone repos.
Cross-cutting *program* memories (bigsim-program, parallel-dev, test-execution-policy,
the notebook-discipline notes, the github-repo pointer) stayed in the BigSim archive store;
`[[links]]` to those will dangle here by design.

One line per memory: a hook for relevance triage — the full detail lives in each topic file.

- [Engine unfrozen](engine-unfrozen.md) — **project (2026-06-10 → framing RETIRED 2026-06-14 `2a8b74e`):** `engines.diffusion` UNFROZEN → governance open + test-gated (ADR 0004); the "fit WITHIN the frozen engine" reflex is now lineage. Three additive amendments landed (prior invariants unmodified): v1.5 native nonlinear `D(u)` (Picard), v1.6 explicit `forward_euler` θ=0, v1.8 2-D. **3-D = the only remaining deferred regime.** **2026-06-14: the whole contract/seal/invariant framing DROPPED → plain tested library** (suite still gates, ceremony gone); `CONTRACT.md` kept at path + retitled "reference", ADR 0004 got a dated `## Follow-up` trace, consumer/historical narratives left intact.
- [Engine explicit stepping v1.6](engine-explicit-stepping-v16.md) — **project (2026-06-11):** explicit `forward_euler` (θ=0) BUILT — falls out of the θ-method (only `_METHODS` lacked it); the real content is the new conditional-CFL invariant (`dt_crit=1/max|diagᵢ|`, NOT textbook `Δx²/2D`). Additive, 28 invariants unmodified. Advisor: build explicit NOT 2-D (anti-over-build).
- [Commit at end of batch](commit-at-end-of-batch.md) — **feedback (2026-06-10):** always commit AND **push** at the end of a work batch without being asked; chip-sim commits direct to `main` → `git push origin main` (solo repo, no PR flow). Conventional message + Co-Authored-By, fast lane green first; PRs still ask-first.
- [Gradual failure preferred](gradual-failure-preferred.md) — **feedback (2026-06-14):** model **graded/gradual failure over cliff** whenever physically realistic (the full ok→rework→fail spectrum, not a coin flip). Honest move = spatial non-uniformity of the offending quantity (often lifts a named scope edge); the fudge = inflating an unrelated variable past its real value. **1st application BUILT:** purification's Na→V_t cliff → an **edge-loaded Na ring** (`Variation.na_factor=1+2.5·r²`, flagged ~3.5× rim) + a **continuous refining lever** (`zone_passes`→float, `k^n` smooth) so a *modest* rim suffices (avoided the 10× fudge); `test_na_ring.py`. Mechanism only — no player stage yet. [[fab-game]] [[scope-edge-backlog]]
- [Chip coupling v1.2](chip-coupling-v12.md) — Microchip v1.2 Phase 1↔2 back-coupling (OED + dopant segregation) BUILT — both fit WITHIN the engine (OED=`D(t)`; segregation=`Neumann(flux(t))`). Swept-sliver edge RETIRED via a consumer-side **receding mesh** (`moving_boundary=True`, engine untouched, no ADR). [[oed-source]] [[dopant-segregation-source]]
- [Chip high-conc D(N) v1.3](chip-highconc-v13.md) — Microchip v1.3 conc-dependent diffusivity `D(N)` (high-conc box) BUILT — numerics SUPERSEDED by v1.5's native `D(u)` (Picard); physics/demo numbers stay. Cited **Fair charge-state** `D⁰+D⁻(n/nᵢ)+D⁼(n/nᵢ)²`; full-activation cap → physical ×42. [[dopant-charge-state-diffusivity-source]]
- [Chip defocus v1.4](litho-defocus-v14.md) — Microchip v1.4 defocus/DOF/Bossung BUILT (`litho.py` §7) — defocus = pure pupil PHASE, fits inside litho, z=0 bit-for-bit seam. Advisor fix: assert the FUNDAMENTAL `4c₀c₁cosφ` not contrast → at φ=π/2 the image frequency-doubles. Unitary conservation. [[litho-aerial-image-source]]
- [Litho PEB v1.7](litho-peb-v17.md) — **project (2026-06-11):** PEB acid-diffusion blur BUILT (`litho.py` §8) — the one engine-free module runs its resist back-end on `engines.diffusion` in acid mode (CN default, pure consumer, no ADR). σ=0 seam; the PEB window closes where the lens out-resolves the bake (`NA(1+σ)/4nc`) → why BARC. [[peb-acid-diffusion-source]]
- [Litho CAR v1.9](litho-car-v19.md) — **project (2026-06-12):** CAR reaction–diffusion PEB BUILT (`litho.py` §9) — INVERTS [[litho-peb-v17]]: coupled two-field (acid+blocked) doesn't fit the single-field engine → consumer-side operator splitting (BE not CN). Cited Kirchauer §7.1.2 (APEX-E). Amplification sharpens; develop on `1−m`.
- [Litho Zernike v1.10](litho-zernike-v110.md) — **project (2026-06-12):** Zernike aberrations (coma/astig/spherical) BUILT (`litho.py` §10) — same finding as [[litho-defocus-v14]]: aberration = pupil PHASE, no new path. THE trap: coma & defocus share `4c₀c₁cosφ`; the discriminator is `fundamental_complex` PHASE → coma = placement error. NO Strehl (needs 2-D disk integral).
- [Chip 2-D device cross-section v1.11](chip-device-2d-v111.md) — **project (2026-06-12):** 2-D MOSFET cross-section BUILT (`chip/device_2d.py`) — wires the v1.8 2-D engine into the Phase-4 device; lateral encroachment → `L_eff=L_drawn−2ΔL`. A **VALIDATION** deepening (independent two-window half-cell confirms the textbook subtraction), not new physics; by-construction legs = guards not anchors; DIBL guarded out. [[lateral-diffusion-source]]
- [Lateral diffusion 2-D v1.8](lateral-diffusion-2d.md) — **project (2026-06-12):** the 2-D regime BUILT — `engines.diffusion` new module `diffusion2d.py` (`Diffusion2D`/`Grid2D`/`MaskedSurface`), BE-only 5-point FV, the engine's LAST deferred regime (consumer: lateral diffusion under a mask edge). Additive, 34 invariants unmodified. Tight anchor = the dimensional-collapse seam; lateral/vertical ratio = LOOSE. [[engine-unfrozen]]
- [Chip notebook flake](chip-notebook-flake.md) — slow `chip.ipynb` smoke test hangs ~80% in CI — an nbclient↔ipykernel-7.x message race during EXECUTION (NOT startup/content). `skipif(CI)` stays; pinning `ipykernel<7` AND the teardown-skip both FAILED. **THE PIN:** `-n auto` rides only the fast lane, full gate stays serial (notebook never under xdist). **Do NOT re-explore fewer workers** (idle cores don't protect it).
- [Deal–Grove oxidation source](deal-grove-oxidation-source.md) — Microchip P2: cited Deal–Grove linear-parabolic oxide-growth rate constants (B, B/A; wet/dry; orientation; 0.44 Si ratio) used by oxidation.py; thin-dry edge since PROMOTED → [[massoud-thin-oxide-source]].
- [Dopant charge-state diffusivity source](dopant-charge-state-diffusivity-source.md) — Microchip v1.3: cited **Fair charge-state** `D(N)=D⁰+D⁻(n/nᵢ)+D⁼(n/nᵢ)²` (the n² term drives the box) + B/P/As/Sb table + `nᵢ(T)` + active-carrier cap; Plummer Ch.7 / **Fair–Tsai JECS 124:1107 (1977)** / Velichko. The conc-enhanced counterpart of [[dopant-diffusivity-source]].
- [Dopant diffusivity source](dopant-diffusivity-source.md) — Microchip P1a: Fair/Plummer intrinsic Arrhenius `D(T)` for B/P/As/Sb in Si + erfc/Gaussian forms + predep dose identity; conc-enhanced `D(N)` edge BUILT in v1.3 → [[dopant-charge-state-diffusivity-source]].
- [Dopant mobility source](dopant-mobility-source.md) — Microchip P1a: **Masetti 1983 (IEEE TED 30:764)** μ(N) carrier-mobility model = the R_s conductance-integral integrand (independent of Irvin → non-circular cross-check).
- [Dopant segregation source](dopant-segregation-source.md) — Microchip v1.2: cited segregation coefficient `m=solubility_Si/solubility_SiO₂` (DEFINITION load-bearing — inverting flips deplete↔pile-up) + B 0.1–0.3 / P 10 (Hollauer TU Wien diss §4.1); flux BC `J=N_surf·(0.44−1/m)·dx_ox/dt`. Used by [[chip-coupling-v12]].
- [Dopant solid solubility source](dopant-solid-solubility-source.md) — Microchip P1a: **Trumbore 1960 (BSTJ 39:205)** solid-solubility limits = the predep Dirichlet surface `N_s` (same ref as the Czochralski segregation `k`).
- [Irvin sheet-resistance source](irvin-sheet-resistance-source.md) — Microchip P1a: **Irvin 1962 (BSTJ 41:387)** R_s·x_j benchmark; GRAPHICAL not callable — code computes R_s via the independent μ(N).
- [Litho aerial-image source](litho-aerial-image-source.md) — Microchip P3: cited Rayleigh k₁ (0.25 two-beam / 0.5 coherent / ~0.28 best) + NILS=CD·d(lnI)/dx printability rule (Mack/lithoguru) that litho.py benchmarks against.
- [PEB acid-diffusion source](peb-acid-diffusion-source.md) — Microchip v1.7: cited PEB model — `σ=√(2Dt)` Gaussian blur + sealed-film Neumann BC (Kirchauer IUE-Vienna §7.1.2 / Mack 1995), standing-wave period λ/2n, half-period smoothing σ≥λ/4n; CAR reaction–diffusion = the named scope edge. Used by [[litho-peb-v17]].
- [Lateral diffusion source](lateral-diffusion-source.md) — Microchip v1.8: cited lateral-diffusion-under-a-mask-edge model — lateral/vertical junction ratio ≈ **0.75–0.85, contour-dependent**, anchored ≈0.82 at C_B/N_s=1e-4 (**Kennedy–O'Brien 1965**, via a secondary fab text). Model runs ~5–10% high → a **LOOSE** benchmark. Used by [[lateral-diffusion-2d]].
- [Massoud thin-oxide source](massoud-thin-oxide-source.md) — Microchip P2 v1.1: cited Massoud thin-dry rapid-growth constants (J.Appl.Phys 62:3416 / Hollauer TUW §2.7) used by oxidation.py §5; the COHERENT-SET rule + the τ SIGN-TYPO finding (exp(+E/kT) not −); dry 800–1000°C only; plain DG stays default/bit-for-bit.
- [MOS threshold-voltage source](mos-threshold-voltage-source.md) — Microchip P4: cited V_t formula + CGS constants + the **MIT 6.012 PS3 P2** worked example (Vt≈0.58V, the conservation cross-check) that device.py reproduces.
- [OED source](oed-source.md) — Microchip v1.2: cited OED — half-power supersaturation `Δ∝(dx_ox/dt)^0.5` (IUE-Vienna Wimmer / Antoniadis-Moskowitz JAP 53:6788 1982) + fractional interstitialcy f_I (B/P/As/Sb); form `D_eff/D_inert=1+f_I·Δ`, amplitude CALIBRATED-flagged. Used by [[chip-coupling-v12]].
- [Fab-line game](fab-game.md) — **project (2026-06-12):** gamified full-line fab simulator (sand→chip) LAYERED on chip-sim (plan `docs/plans/fab-game.md` + ADR 0005): physics in `chip/`+`engines/` (triads), game in `fab_game/` (one-way import). **G1–G7 all BUILT** (full line sand→binned chip + scored roguelike shell): [[fab-game-g1]] harness, [[fab-game-g2]] Czochralski/Scheil boule, [[fab-game-g3]] wafer-prep yield+geometry, [[fab-game-g4]] purification, [[fab-game-g4b]] SRH lifetime/leakage, [[fab-game-g5]] etch/depo, [[fab-game-g6]] packaging/binning, [[fab-game-g7]] scoring shell. **CG-1/2/3 all BUILT** (crystal-growth deepenings): [[fab-game-cg1]] BPS k_eff, [[fab-game-cg2]] Voronkov V/G, [[fab-game-cg3]] Stefan gradient. **Scope-edge promotions BUILT:** [[fab-game-c1]] thermal donors, [[fab-game-d1]] under-etch (the [[scope-edge-backlog]] C1/D1 pair), [[fab-game-a2]] OSF ring (CG-2 made radial, across-wafer), [[fab-game-a1]] interstitial dislocations→leakage (CG-2's mirror; two-sided window), [[fab-game-e1]] spike/RTA `D(T(t))` thermal-budget path (the OED `effective_Dt` twin — and the move that **closed the heat-mode search: heat-mode FALSIFIED chip-side → Steel-only**). **The scope-edge backlog is now exhausted — every remaining edge lacks a consumer.** **§9 guided ipywidgets dashboard BUILT (2026-06-14)** = the deferred Textual TUI's headless, tested core (`fab_game/dashboard.py` + `plots.dashboard_figure` + the `fab_game.ipynb` "command the whole line" section; `run_dashboard`/`dashboard_summary`, 4 knobs, Variation ON, reuses `LineResult`). **Textual TUI v1 BUILT (2026-06-14)** = `fab_game/tui.py` (`FabLineApp`) thin-driving that core + new headless `plots.wafer_map_text`; only `textual` importer, NOT in `__init__` (fast lane headless), `[tui]` extra, `importorskip`+`run_test()` pilot — 637 green, no xdist flake. **Textual TUI v2 — the G7 roguelike loop — BUILT (2026-06-14)** = new headless `fab_game/session_view.py` renderers (re-exported, tested without textual) + a `RoguelikeScreen` in `tui.py` driving `game.py`'s `GameSession` down one boule (oxide *adapt* lever Input + Process/Scrap Buttons, `session.done`-guarded; bug found: `_render` shadowed Textual's internal → renamed `_repaint`); pilot pins fidelity (driven session == headless `play(...)`), 651 green. **Educational/Hardcore launch mode BUILT (2026-06-14)** = `python -m fab_game.tui` opens a `ModeSelectScreen`; Hardcore = today's TUI byte-identical, Educational layers a verbatim **guide panel** (every selector + readout explained — defocus/slice z/oxide drive/seed/V_t/I_Dsat/NILS/CD/leakage/Scheil — plus what-to-do) from a NEW import-pure `fab_game/guide.py` (`dashboard_guide`/`roguelike_guide`/`glossary_text`/`MODE_INTRO`, tested without textual); presentation-only (seam byte-identical, guide `display:none` in hardcore), two flags `FabLineApp(educational, prompt_mode)` keep the pilots untouched, import-purity tested in a subprocess. **Input-domain crash guard BUILT (2026-06-14)** = a parseable-but-out-of-domain knob (`slice_z`∉[0,1), oxide≤0) used to raise into the Textual handler + kill the App; fix = headless `dashboard.knob_errors`/`oxide_minutes_error` + belt-and-suspenders pre-screen + `try/except` net, catch scoped by class `(ValueError, ArithmeticError)` (OverflowError from `int(float("1e999"))`), pilot = the real regression. Remaining front-end = the **tycoon ONLY** (roguelike loop + educational mode now built); "more of a game" (more screens/challenge) = a still-open **design conversation**. [[engine-unfrozen]]
- [Scope-edge backlog](scope-edge-backlog.md) — **project (2026-06-14):** `docs/plans/scope-edge-backlog.md` triages the whole bag of named-but-unbuilt edges **by consumer** (spine = the deferrals, the anti-over-build bar). **✅ BUILT:** oxygen/thermal donors → [[fab-game-c1]], under-etch → [[fab-game-d1]], **A2 OSF ring → [[fab-game-a2]]** (ring half — CG-2 made radial, closed-form), **A1 interstitial→leakage → [[fab-game-a1]]** (CG-2's mirror, two-sided window, also A2's leaky-rim consumer). **A2 SPLIT (2026-06-14):** ring BUILT; **Robin-`G` sourcing FALSIFIED/deferred** — Voronkov needs a STEADY gradient (closed-form), the shipped engine can't beat it (no field-coupled sink → straight line; no `1/r`); heat-mode's home is the Steel program. **E1 SPLIT/BUILT (2026-06-14) → [[fab-game-e1]]:** the `D(t)` thermal-budget path BUILT; the emergent-`T` **heat-mode clause FALSIFIED** (`√(D/α)≈1e-6` → `T` is the setpoint over the junction; joins Robin-`G`) → **NO chip-side heat-mode consumer → Steel-only**. **Next promotable = NONE** (the backlog is now exhausted — every remaining edge lacks a consumer). **Deferred (no consumer):** striations, facets, transient Stefan `X(t)` (= the only place a transient heat solve bites — melt laser anneal, liquid-`D`, not E1), engine 3-D, CMP, rebond, Robin-`G`. [[fab-game]] [[engine-unfrozen]]
- [Fab-game C1](fab-game-c1.md) — **project (2026-06-14):** crucible oxygen → **thermal donors** BUILT (`czochralski.py` §1e) — the crystal-growth ELECTRICAL deepening. Cited **Kaiser–Frisch–Reiss fourth-power** initial rate `∝[O_i]⁴` (Phys.Rev.112:1546, web-verified); donors compensate the p-substrate (`N_A−N_TD`, exact) → V_t down via the G4a chain. Seam by BOTH paths (no oxygen OR no anneal). Cube saturation + magnitudes flagged; type inversion guarded. [[scope-edge-backlog]] [[fab-game-g4]]
- [Fab-game D1](fab-game-d1.md) — **project (2026-06-14):** **under-etch** BUILT (`etch_deposition.py` §3) — closes G5's over/under-etch pair. Incomplete clear → `residual=UE·film` → **bridges** the gate lines into a functional SHORT (mirror of the void's OPEN); flagged threshold, mutual-exclusion guard with over-etch, CD untouched (purely functional). Demo = the etch **process window** (Goldilocks, bracketed short↔open). [[scope-edge-backlog]] [[fab-game-g5]]
- [Fab-game A2](fab-game-a2.md) — **project (2026-06-14):** **OSF ring = CG-2 made RADIAL** BUILT (`czochralski.py` §1f) — a radial `G(r)=G_center·(1+boost·r²)`→`ξ(r)` FALLS outward→ V/I boundary `osf_ring_radius`. Per-die killer density keyed on `radius_frac` (`scatter_defects` gained `density_fn`). **THE finding (advisor):** monotone `void_density` peaks at CENTRE, zero at ring → a dead vacancy **CORE + clean rim, NOT a ring of kills** (the ring is where kills STOP). Tight = ring LOCATION (coefficient-robust) + topology signs; flagged = profile/existence. No engine (Robin-`G` falsified). `boost=None`→CG-2 byte-for-byte (seam proven). [[scope-edge-backlog]] [[fab-game-cg2]] [[fab-game-g3]]
- [Fab-game A1](fab-game-a1.md) — **project (2026-06-14):** **interstitial side of Voronkov** BUILT (`czochralski.py` §1g `dislocation_defect_density` = void-density MIRROR across ξ_t; `lifetime.py` `dislocation_recombination_rate`, `1/τ+=K·ρ_disl`) — slow pull (ξ<ξ_t) → dislocations → junction **LEAKAGE** (G4b channel), NOT yield. **Two-sided window** (fast→yield, slow→leakage, optimum AT ξ_t); A1 = A2's deferred leaky-rim consumer → **OSF ring = the one annulus clean of BOTH**. NO new knob (reads existing V,G; both `device_step` sites), V_t bystander, ρ_disl=0 seam byte-for-byte. Product of 2 flagged coeffs = the 1 magnitude. Corner (realistic CZ vacancy-side). No engine/conservation. [[fab-game-cg2]] [[fab-game-a2]] [[fab-game-g4b]] [[scope-edge-backlog]]
- [Fab-game E1](fab-game-e1.md) — **project (2026-06-14):** **transient spike/RTA anneal `T(t)`→`D(T(t))` SPLIT** — heat-mode FALSIFIED, `D(t)` budget BUILT (`diffusion_dopant.py` §4). THE gate ("is `T` emergent or setpoint?") → SETPOINT: `√(D_dopant/α_thermal)≈1.2e-6` → `T` flat over the junction → `D(T(t))` **IS** the shipped `D(t)` (the OED `effective_Dt` twin), NOT a heat-mode engine. Heat-mode joins Robin-`G` falsified → **NO chip-side heat-mode consumer → Steel-only**. BUILT: `ThermalProgram`/`thermal_budget`/`equivalent_isothermal_time`/`drive_in_program`/`spike_budget_time_laplace`; `DiffusionKnobs.drivein_program` (opt-in seam). Finding: budget collapses near the peak (7×; top 50°C=84%) → faster ramp → smaller `∫D dt` → shallower `x_j` (why RTA). Seam bit-for-bit; equivalence-inverse tight; Laplace `t_eq` ~5%, `D0`-indep exact. No engine/ADR. **Backlog now exhausted (Next promotable=NONE).** [[scope-edge-backlog]] [[fab-game-a2]] [[oed-source]] [[chip-coupling-v12]]
- [Fab-game G4b](fab-game-g4b.md) — **project (2026-06-13):** `chip/lifetime.py` SRH lifetime `1/τ=1/τ_bulk+Σσ_n·v_th·N_metal` + generation leakage `J∝N_metal` — deep-level metals (Fe/Cu) become the device consequence net doping can't carry. Triad: tight SRH machinery (low-injection limit; detailed-balance `U=0`), magnitudes flagged. Metals never touch V_t; rework = more passes. [[fab-game-g4]]
- [Fab-game G4a](fab-game-g4.md) — **project (2026-06-13):** silicon purification + Tier-1 contamination model — cited `chip/purification.py` **Pfann single-pass** zone refining `C(u)=C₀[1−(1−k)e^(−ku)]`. Conservation REFRAMED (single-pass `∫C` falls short, a named edge — unlike Scheil). Na→Q_ox→V_t DOWN (lifts the `Q_ox=0` edge); default `grade="clean"` → seam. [[fab-game-g3]]
- [Fab-game G1](fab-game-g1.md) — **project (2026-06-12):** `fab_game/` harness wired through the validated back end, ZERO new physics; banks "one bad knob → dead die + failure trail". 5 ADR-0005 §5 invariants green (seam/determinism/propagation/bookkeeping/import-guard). Finding: defocus's casualty = **NILS not CD**; V_t never on the defocus chain. [[fab-game]]
- [Fab-game G2](fab-game-g2.md) — **project (2026-06-12):** `chip/czochralski.py` Scheil axial segregation `C_s(z)=N_seed·(1−z)^(k−1)` + Boule wired into `fab_game`. Seam fix: parameterize by **seed-end** `N_seed` not melt `C_0` (float trap). Unit-of-run RESOLVED (run = wafer at `slice_z`). Boron k=0.8 → V_t walks 0.55→0.75, out of spec by z≈0.8. [[fab-game-g1]]
- [Fab-game G3](fab-game-g3.md) — **project (2026-06-13):** wafer prep — cited `chip/wafer_prep.py` defect-limited yield `Y=exp(−D₀A)` (Murphy/Poisson; Stapper NB `α→∞` limit) + TTV/bow geometry. `fab_game/defects.py` per-die Poisson scatter **converges to the cited law**. Killer = functional fail; default `defect_density=0` → seam. [[fab-game-g2]]
- [Fab-game G5](fab-game-g5.md) — **project (2026-06-13):** mid-line etch & deposition (`chip/etch_deposition.py`), the first **flagged-phenomenology** tier (no conservation invariant). Etch bias shrinks CD; deposition void when gap AR > `AR_crit`. One tight leg = the bit-for-bit seam (A=1, SC=1); magnitudes flagged. Etch overwrites `cd_nm`; void = functional kill. [[fab-game-g4]]
- [Fab-game G6](fab-game-g6.md) — **project (2026-06-13):** back end — cumulative assembly-yield funnel `Y=Πyᵢ` (cited `chip/packaging.py`) + game-side speed binning. Trap: binning is a grading policy NOT physics → dropped Gaussian `bin_fractions`, packaging single-headed. Assembly kill = per-die Bernoulli drawn LAST → G1–G5 byte-identical. Line complete sand→binned chip. [[fab-game-g5]]
- [Fab-game G7](fab-game-g7.md) — **project (2026-06-13):** roguelike game shell (`fab_game/scoring.py`+`game.py`), purely additive game policy (no physics touched). Framing: one boule = one run, the G2 Scheil V_t drift IS the difficulty curve. Lever = thin the gate oxide (Goldilocks LIMIT: over-thinning hits the I_Dsat ceiling). TUI deferred. [[fab-game-g6]]
- [Fab-game CG-1](fab-game-cg1.md) — **project (2026-06-13):** Czochralski deepening #1 — cited **Burton–Prim–Slichter** `effective_segregation_coefficient(k₀,Δ)=k₀/[k₀+(1−k₀)e^(−Δ)]` (`czochralski.py` §1b). Pull rate lifts k_eff→1 → flattens the Scheil drift. Triad: two LIMITS tight, no conservation law. Honest: boron barely moves at realistic pull. Opt-in, seam-safe. [[fab-game-g2]]
- [Fab-game CG-2](fab-game-cg2.md) — **project (2026-06-13):** deepening #2 — Voronkov `V/G` grown-in defect criterion (`czochralski.py` §1c; ξ>ξ_t ⇒ vacancy/void COP killers), feeds the **same G3 Poisson map** — the in-model brake CG-1 lacked. Triad = criterion + ξ_t cited, no conservation law. Finding: CG-1 benefit flat → CG-2 alone sets the pull. Opt-in, seam-safe. [[fab-game-cg1]] [[fab-game-g3]]
- [Fab-game CG-3](fab-game-cg3.md) — **project (2026-06-13, last CG deepening):** Stefan interface balance supplies CG-2's gradient `G`; `ξ=V/G_s` **saturates** at `ξ_max=k_s/(L·ρ)≈0.32` (caps the vacancy cost). Closed-form, NO engine/ADR (anti-over-build). Triad = limits + cited Si constants, NO conservation law (the flux round-trip is by-construction). [[fab-game-cg2]]
- [Gallery local edition](gallery-local-edition.md) — **project (2026-06-12 → 2026-06-14 `0b3440c`):** TWO galleries now, each public + local (4 pages). chip: `docs/index.html` (public/GitHub) + `index.local.html` (running JupyterLab `localhost:8888` → click→live-launch) from `chip/gallery.py`. **NEW sibling for the game layer:** `docs/fab-game.html` + `fab-game.local.html` from **`fab_game/gallery.py`** — surfaces the 16 `fab-game-*.png` the chip gallery never linked (the audit finding); lives in `fab_game/` for the ADR 0005 boundary, reuses chip's pkg-parametrized primitives, cross-linked both ways. Standalone-Jupyter user. All golden-tested; **don't delete the .local pages / don't mix github↔localhost**. README still narrates only G1–G5 (open).
